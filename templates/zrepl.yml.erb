# THIS FILE IS MANAGED BY PUPPET
<% require 'yaml' -%>
<% global = scope.lookupvar('zrepl::global') -%>
<% jobs = scope.lookupvar('zrepl::jobs') -%>
<% full_config = {
  'global'=>global,
  'jobs'=>jobs,
}
 -%>
<%=
full_config.to_yaml()
 -%>
